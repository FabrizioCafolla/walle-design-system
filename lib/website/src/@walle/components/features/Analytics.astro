---
import config from "@walle/config";

interface Props {
  enable?: boolean;
}

const { enable = true } = Astro.props;
---

{config.env.IS_PRODUCTION && enable && "<add you analytics script snippet>"}
